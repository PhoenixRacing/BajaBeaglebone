from pydispatch import dispatcher
import time
from random import randint

senderA = "PubA"
SIGNAL = 101

def sendMessageA():
    while True:
        dispatcher.send(signal=SIGNAL, sender=senderA)
        time.sleep(1)
